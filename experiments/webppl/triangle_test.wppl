var check = function(a, b, c) {
  if ((a <= 0) || (b <= 0) || (c <= 0)) {
      return 3;
  }
  else if ((a+b<=c) || (a+c<=b) || (b+c <= a)) {
    return 3;
  }/*
  else if ((a == b) && (b == c) && (a == c)) {
    return 2;
  }
  else if ((a == b) || (b == c) || (a == c)) {
    return 1;
  }*/
  else {
    return 0;
  }
}

var check2 = function(a, b, c) {
  if ((a <= 50) || (b <= 50) || (c <= 50)) {
      return 3;
  }
  else {
    return 0;
  }
}

var check3_1 = function(a, b, c) {
  if (a+b<=c+50) {
    return 3;
  }
  else {
    return 0;
  }
}

var check3_2 = function(a, b, c) {
  if ((a+b<=c+50) || (a+c<=b+50)) {
    return 3;
  }
  else {
    return 0;
  }
}


var check3 = function(a, b, c) {
  if ((a+b<=c+50) || (a+c<=b+50) || (b+c <= a+50)) {
    return 3;
  }
  else {
    return 0;
  }
}

var check4 = function(a, b, c) {
  if ((a <= 50) || (b <= 50) || (c <= 50)) {
      return 3;
  }
  else if ((a+b<=c+50) || (a+c<=b+50) || (b+c <= a+50)) {
    return 3;
  }
  else {
    return 0;
  }
}

var main = function(){
  /*
  var a = randomInteger(101) - 50;
  var b = randomInteger(101) - 50;
  var c = randomInteger(101) - 50;
  return check(a, b, c);
  */
  var a = randomInteger(101);
  var b = randomInteger(101);
  var c = randomInteger(101);
  
  
  // return check2(a, b, c);
  // return check3_1(a, b, c);
  return check3_2(a, b, c);
  // return check3(a, b, c);
  // return check4(a, b, c);
};

Infer({method:'enumerate',model:main}).getDist(0)
